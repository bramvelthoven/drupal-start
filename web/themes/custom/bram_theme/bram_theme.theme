<?php

/**
 * @file
 * Functions to support theming.
 */

/**
 * Load include files which contain additional theming logic.
 */
foreach (glob(\Drupal::service('extension.list.theme')->getPath('bram_theme') . '/includes/*.theme') as $file) {
  include $file;
}
