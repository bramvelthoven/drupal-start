{% include active_theme() ~ ':slider' with {
  'title': 'Lorum ipsum',
  'items': [
    drupal_image(2),
    drupal_image(2),
    drupal_image(2),
    drupal_image(2),
    drupal_image(2)
  ]
} only %}
